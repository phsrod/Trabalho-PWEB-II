server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index /web/index/login.html;

    location /web/ {
        alias /usr/share/nginx/html/web/;
    }

    location / {
        try_files $uri $uri/ /web/index/login.html;
    }
}
